schema_version: 1

context:
  name: ibmcloudant
  version: "0.11.8"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/ibmcloudant-${{ version }}.tar.gz
  sha256: a792018057eeffadab7b36214b774528492885c391e0fce6e31f6c2d929a388e

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - flit-core >=3.9,<4
    - pip
  run:
    - python >=${{ python_min }}
    - ibm-cloud-sdk-core ==3.24.4
    - requests >=2.32.4,<3.0.0
    - python-dateutil >=2.5.3,<3.0.0
    - pyjwt >=2.0.1,<3.0.0

tests:
  - python:
      imports:
        - ibmcloudant
      python_version: ${{ python_min }}.*
      pip_check: true

about:
  summary: Python client library for IBM Cloudant
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://github.com/IBM/cloudant-python-sdk
  repository: https://github.com/IBM/cloudant-python-sdk
  documentation: https://github.com/IBM/cloudant-python-sdk/blob/main/README.md

extra:
  recipe-maintainers:
    - xylar
